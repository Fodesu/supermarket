{
  "name": "memohai-supermarket",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "bun test",
    "typecheck": "bun run types && bun run typecheck:server && bun run typecheck:app",
    "typecheck:server": "tsc --noEmit --project tsconfig.check.json",
    "typecheck:app": "vue-tsc --noEmit --project tsconfig.app.json",
    "types": "bun run types:api && bun run types:writer",
    "types:api": "mkdir -p .wrangler/types && wrangler types .wrangler/types/api.d.ts --config workers/api/wrangler.jsonc --env production --env-interface ApiEnv",
    "types:writer": "mkdir -p .wrangler/types && wrangler types .wrangler/types/writer.d.ts --config workers/writer/wrangler.jsonc --env production --env-interface WriterEnv --include-runtime false",
    "registry:refresh": "bun scripts/registry/refresh.ts",
    "registry:gc": "bun scripts/registry/gc.ts",
    "registry:validate": "bun scripts/registry/validate.ts",
    "registry:client": "bun client/skill-registry-client.ts",
    "registry:writer:dev": "wrangler dev --config workers/writer/wrangler.jsonc --env test --test-scheduled",
    "registry:writer:check": "wrangler deploy --config workers/writer/wrangler.jsonc --env test --dry-run --containers-rollout=none",
    "registry:writer:deploy:test": "wrangler deploy --config workers/writer/wrangler.jsonc --env test",
    "registry:writer:deploy:production": "wrangler deploy --config workers/writer/wrangler.jsonc --env production",
    "registry:api:dev": "bun run build && wrangler dev --config workers/api/wrangler.jsonc --env test",
    "registry:api:deploy:test": "bun run build && wrangler deploy --config workers/api/wrangler.jsonc --env test",
    "registry:api:deploy:production": "bun run build && wrangler deploy --config workers/api/wrangler.jsonc --env production"
  },
  "imports": {
    "#*": "./*"
  },
  "devDependencies": {
    "@cloudflare/containers": "^0.3.7",
    "@vitejs/plugin-vue": "^6.0.5",
    "nitro": "latest",
    "typescript": "^5.9.3",
    "vite": "^8.0.2",
    "vue": "^3.5.30",
    "vue-tsc": "^3.3.8",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "yaml": "^2.8.3"
  }
}
